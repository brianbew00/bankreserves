import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Bank Reserves | FDIC Financial Data Analytics",
  description: "Professional FDIC bank financial data viewer. Search banks, analyze reserves, liquidity ratios, and financial metrics with real-time data from the Federal Deposit Insurance Corporation.",
  keywords: "FDIC, bank reserves, financial data, liquidity ratios, banking analytics, federal deposit insurance",
  authors: [{ name: "Bank Reserves Analytics" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
